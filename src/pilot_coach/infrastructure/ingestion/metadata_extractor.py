class MetadataExtractor:
    def extract(self, text: str) -> dict:
        # Expand this into ACS-aware parsing later.
        return {
            "source_title": "FAA Source",
            "section": None,
            "page": None,
            "acs_code": None,
        }
